Hello <%= (@resource.respond_to?(:name) && @resource.name.present?) ? @resource.name : @resource.email %>!
<p>
<%-if @resource.supplier.invite %>
  <%= raw @resource.supplier.invite %>
<%- else %>
<%= @resource.supplier.name %> has invited you to FromWhereItsGrown.com
</p><p>
FromWhereItsGrown.com allows you to change your existing and future orders with <%= @resource.supplier.name %> at any time without having to call. 
</p>
<%- end %>
<p>
You can accept the invitation through this link:
<%= link_to 'Accept invitation', accept_invitation_url(@resource, :invitation_token => @resource.invitation_token) %>
</p><p>
Once you have accepted the invitation you can access the site through this link:
<% if @resource.supplier.embed? %>
  <%= link_to @resource.supplier.parent_url, @resource.supplier.parent_url %>
<% else %>
  <%= link_to home_url, home_url %>
<% end %>
</p><p>
If you don't want to accept the invitation, please ignore this email.
Your account won't be created until you access the link above and set your password.
</p>
